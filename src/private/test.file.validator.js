const { body, param, query, validationResult } = require('express-validator');

const handleValidationErrors = (req, res, next) => {
    const errors = validationResult(req);
    if (!errors.isEmpty()) {
        console.log('Validation Errors:', JSON.stringify(errors.array(), null, 2));
        return res.status(400).json({
            success: false,
            message: 'Validation failed',
            errors: errors.array()
        });
    }
    console.log('Validation passed successfully');
    next();
};

const createTestFile = [
    body('project')
        .notEmpty().withMessage('Project ID is required')
        .isMongoId().withMessage('Invalid project ID format'),

    body('folder')
        .notEmpty().withMessage('Folder ID is required')
        .isMongoId().withMessage('Invalid folder ID format'),

    body('name')
        .notEmpty().withMessage('File name is required')
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    body('fileName')
        .notEmpty().withMessage('File name with extension is required')
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    body('path')
        .notEmpty().withMessage('File path is required')
        .trim(),

    body('extension')
        .notEmpty().withMessage('File extension is required')
        .trim()
        .isLength({ min: 1, max: 20 }).withMessage('Extension must be between 1 and 20 characters'),

    body('type')
        .optional()
        .isIn(['test', 'feature', 'step_definition', 'config', 'util', 'helper', 'data', 'pom', 'testng', 'properties', 'java', 'xml', 'json', 'yaml', 'markdown', 'other'])
        .withMessage('Invalid file type'),

    body('language')
        .optional()
        .isIn(['java', 'javascript', 'typescript', 'python', 'xml', 'json', 'yaml', 'properties', 'gherkin', 'markdown', 'text'])
        .withMessage('Invalid language'),

    body('content')
        .notEmpty().withMessage('File content is required')
        .isString().withMessage('Content must be a string'),

    body('encoding')
        .optional()
        .isString().withMessage('Encoding must be a string'),

    body('metadata.className')
        .optional()
        .trim(),

    body('metadata.packageName')
        .optional()
        .trim(),

    body('metadata.imports')
        .optional()
        .isArray().withMessage('Imports must be an array'),

    body('metadata.methods')
        .optional()
        .isArray().withMessage('Methods must be an array'),

    body('metadata.methods.*.name')
        .optional()
        .isString().withMessage('Method name must be a string'),

    body('metadata.annotations')
        .optional()
        .isArray().withMessage('Annotations must be an array'),

    body('metadata.dependencies')
        .optional()
        .isArray().withMessage('Dependencies must be an array'),

    body('isGenerated')
        .optional()
        .isBoolean().withMessage('Is generated must be a boolean'),

    body('generatedBy')
        .optional()
        .isIn(['ai', 'user', 'system'])
        .withMessage('Generated by must be ai, user, or system'),

    body('aiProvider')
        .optional()
        .isIn(['openai', 'anthropic'])
        .withMessage('AI provider must be openai or anthropic'),

    body('isEditable')
        .optional()
        .isBoolean().withMessage('Is editable must be a boolean'),

    body('isSystemFile')
        .optional()
        .isBoolean().withMessage('Is system file must be a boolean'),

    body('tags')
        .optional()
        .isArray().withMessage('Tags must be an array'),

    body('tags.*')
        .optional()
        .isString().withMessage('Each tag must be a string')
        .trim(),

    (req, res, next) => {
        console.log('Create Test File - Validation started');
        console.log('Request Body:', JSON.stringify(req.body, null, 2));
        handleValidationErrors(req, res, next);
    }
];

const updateTestFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('name')
        .optional()
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    body('fileName')
        .optional()
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    body('path')
        .optional()
        .trim(),

    body('extension')
        .optional()
        .trim()
        .isLength({ min: 1, max: 20 }).withMessage('Extension must be between 1 and 20 characters'),

    body('type')
        .optional()
        .isIn(['test', 'feature', 'step_definition', 'config', 'util', 'helper', 'data', 'pom', 'testng', 'properties', 'java', 'xml', 'json', 'yaml', 'markdown', 'other'])
        .withMessage('Invalid file type'),

    body('language')
        .optional()
        .isIn(['java', 'javascript', 'typescript', 'python', 'xml', 'json', 'yaml', 'properties', 'gherkin', 'markdown', 'text'])
        .withMessage('Invalid language'),

    body('content')
        .optional()
        .isString().withMessage('Content must be a string'),

    body('status')
        .optional()
        .isIn(['draft', 'modified', 'validated', 'compiled', 'error'])
        .withMessage('Invalid status'),

    body('metadata.className')
        .optional()
        .trim(),

    body('metadata.packageName')
        .optional()
        .trim(),

    body('metadata.imports')
        .optional()
        .isArray().withMessage('Imports must be an array'),

    body('metadata.methods')
        .optional()
        .isArray().withMessage('Methods must be an array'),

    body('metadata.annotations')
        .optional()
        .isArray().withMessage('Annotations must be an array'),

    body('metadata.dependencies')
        .optional()
        .isArray().withMessage('Dependencies must be an array'),

    body('isEditable')
        .optional()
        .isBoolean().withMessage('Is editable must be a boolean'),

    body('tags')
        .optional()
        .isArray().withMessage('Tags must be an array'),

    (req, res, next) => {
        console.log('Update Test File - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Request Body:', JSON.stringify(req.body, null, 2));
        handleValidationErrors(req, res, next);
    }
];

const getTestFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Get Test File - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const deleteTestFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('permanent')
        .optional()
        .isBoolean().withMessage('Permanent must be a boolean'),

    (req, res, next) => {
        console.log('Delete Test File - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Permanent Delete:', req.body.permanent);
        handleValidationErrors(req, res, next);
    }
];

const listTestFiles = [
    query('project')
        .optional()
        .isMongoId().withMessage('Invalid project ID format'),

    query('folder')
        .optional()
        .isMongoId().withMessage('Invalid folder ID format'),

    query('type')
        .optional()
        .isIn(['test', 'feature', 'step_definition', 'config', 'util', 'helper', 'data', 'pom', 'testng', 'properties', 'java', 'xml', 'json', 'yaml', 'markdown', 'other'])
        .withMessage('Invalid file type'),

    query('language')
        .optional()
        .isIn(['java', 'javascript', 'typescript', 'python', 'xml', 'json', 'yaml', 'properties', 'gherkin', 'markdown', 'text'])
        .withMessage('Invalid language'),

    query('status')
        .optional()
        .isIn(['draft', 'modified', 'validated', 'compiled', 'error'])
        .withMessage('Invalid status'),

    query('isGenerated')
        .optional()
        .isBoolean().withMessage('Is generated must be a boolean'),

    query('generatedBy')
        .optional()
        .isIn(['ai', 'user', 'system'])
        .withMessage('Generated by must be ai, user, or system'),

    query('isSystemFile')
        .optional()
        .isBoolean().withMessage('Is system file must be a boolean'),

    query('isLocked')
        .optional()
        .isBoolean().withMessage('Is locked must be a boolean'),

    query('extension')
        .optional()
        .trim(),

    query('search')
        .optional()
        .trim(),

    query('tags')
        .optional()
        .trim(),

    query('page')
        .optional()
        .isInt({ min: 1 }).withMessage('Page must be a positive integer'),

    query('limit')
        .optional()
        .isInt({ min: 1, max: 100 }).withMessage('Limit must be between 1 and 100'),

    query('sortBy')
        .optional()
        .isString().withMessage('Sort by must be a string'),

    query('sortOrder')
        .optional()
        .isIn(['asc', 'desc', '1', '-1'])
        .withMessage('Sort order must be asc, desc, 1, or -1'),

    (req, res, next) => {
        console.log('List Test Files - Validation started');
        console.log('Query Parameters:', JSON.stringify(req.query, null, 2));
        handleValidationErrors(req, res, next);
    }
];

const updateFileContent = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('content')
        .notEmpty().withMessage('Content is required')
        .isString().withMessage('Content must be a string'),

    (req, res, next) => {
        console.log('Update File Content - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Content Length:', req.body.content?.length);
        handleValidationErrors(req, res, next);
    }
];

const lockFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Lock File - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const unlockFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Unlock File - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const validateFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Validate File - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const compileFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Compile File - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const restoreFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Restore File - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const getFileVersions = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    query('limit')
        .optional()
        .isInt({ min: 1, max: 100 }).withMessage('Limit must be between 1 and 100'),

    (req, res, next) => {
        console.log('Get File Versions - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Limit:', req.query.limit);
        handleValidationErrors(req, res, next);
    }
];

const revertToVersion = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('version')
        .notEmpty().withMessage('Version number is required')
        .isInt({ min: 1 }).withMessage('Version must be a positive integer'),

    (req, res, next) => {
        console.log('Revert To Version - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Version:', req.body.version);
        handleValidationErrors(req, res, next);
    }
];

const addSyntaxError = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('line')
        .notEmpty().withMessage('Line number is required')
        .isInt({ min: 1 }).withMessage('Line must be a positive integer'),

    body('column')
        .optional()
        .isInt({ min: 0 }).withMessage('Column must be a non-negative integer'),

    body('message')
        .notEmpty().withMessage('Error message is required')
        .trim()
        .isLength({ min: 1, max: 500 }).withMessage('Message must be between 1 and 500 characters'),

    body('severity')
        .optional()
        .isIn(['error', 'warning', 'info'])
        .withMessage('Severity must be error, warning, or info'),

    (req, res, next) => {
        console.log('Add Syntax Error - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Request Body:', JSON.stringify(req.body, null, 2));
        handleValidationErrors(req, res, next);
    }
];

const clearSyntaxErrors = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Clear Syntax Errors - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const updateMetadata = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('className')
        .optional()
        .trim(),

    body('packageName')
        .optional()
        .trim(),

    body('imports')
        .optional()
        .isArray().withMessage('Imports must be an array'),

    body('imports.*')
        .optional()
        .isString().withMessage('Each import must be a string')
        .trim(),

    body('methods')
        .optional()
        .isArray().withMessage('Methods must be an array'),

    body('methods.*.name')
        .optional()
        .isString().withMessage('Method name must be a string'),

    body('methods.*.annotations')
        .optional()
        .isArray().withMessage('Annotations must be an array'),

    body('methods.*.parameters')
        .optional()
        .isArray().withMessage('Parameters must be an array'),

    body('annotations')
        .optional()
        .isArray().withMessage('Annotations must be an array'),

    body('annotations.*')
        .optional()
        .isString().withMessage('Each annotation must be a string')
        .trim(),

    body('dependencies')
        .optional()
        .isArray().withMessage('Dependencies must be an array'),

    body('dependencies.*')
        .optional()
        .isString().withMessage('Each dependency must be a string')
        .trim(),

    (req, res, next) => {
        console.log('Update Metadata - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Request Body:', JSON.stringify(req.body, null, 2));
        handleValidationErrors(req, res, next);
    }
];

const duplicateFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('name')
        .notEmpty().withMessage('New file name is required')
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    body('folder')
        .optional()
        .isMongoId().withMessage('Invalid folder ID format'),

    (req, res, next) => {
        console.log('Duplicate File - Validation started');
        console.log('File ID:', req.params.id);
        console.log('New Name:', req.body.name);
        handleValidationErrors(req, res, next);
    }
];

const moveFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('folder')
        .notEmpty().withMessage('Target folder ID is required')
        .isMongoId().withMessage('Invalid folder ID format'),

    body('path')
        .notEmpty().withMessage('New path is required')
        .trim(),

    (req, res, next) => {
        console.log('Move File - Validation started');
        console.log('File ID:', req.params.id);
        console.log('Target Folder:', req.body.folder);
        console.log('New Path:', req.body.path);
        handleValidationErrors(req, res, next);
    }
];

const renameFile = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    body('name')
        .notEmpty().withMessage('New file name is required')
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    body('fileName')
        .notEmpty().withMessage('New file name with extension is required')
        .trim()
        .isLength({ min: 1, max: 255 }).withMessage('File name must be between 1 and 255 characters'),

    (req, res, next) => {
        console.log('Rename File - Validation started');
        console.log('File ID:', req.params.id);
        console.log('New Name:', req.body.name);
        handleValidationErrors(req, res, next);
    }
];

const getFileStatistics = [
    param('id')
        .notEmpty().withMessage('File ID is required')
        .isMongoId().withMessage('Invalid file ID format'),

    (req, res, next) => {
        console.log('Get File Statistics - Validation started');
        console.log('File ID:', req.params.id);
        handleValidationErrors(req, res, next);
    }
];

const bulkUpdateFiles = [
    body('fileIds')
        .notEmpty().withMessage('File IDs are required')
        .isArray().withMessage('File IDs must be an array'),

    body('fileIds.*')
        .isMongoId().withMessage('Each file ID must be valid'),

    body('updates')
        .notEmpty().withMessage('Updates are required')
        .isObject().withMessage('Updates must be an object'),

    body('updates.type')
        .optional()
        .isIn(['test', 'feature', 'step_definition', 'config', 'util', 'helper', 'data', 'pom', 'testng', 'properties', 'java', 'xml', 'json', 'yaml', 'markdown', 'other'])
        .withMessage('Invalid file type'),

    body('updates.status')
        .optional()
        .isIn(['draft', 'modified', 'validated', 'compiled', 'error'])
        .withMessage('Invalid status'),

    body('updates.tags')
        .optional()
        .isArray().withMessage('Tags must be an array'),

    (req, res, next) => {
        console.log('Bulk Update Files - Validation started');
        console.log('File IDs Count:', req.body.fileIds?.length);
        console.log('Updates:', JSON.stringify(req.body.updates, null, 2));
        handleValidationErrors(req, res, next);
    }
];

const bulkDeleteFiles = [
    body('fileIds')
        .notEmpty().withMessage('File IDs are required')
        .isArray().withMessage('File IDs must be an array'),

    body('fileIds.*')
        .isMongoId().withMessage('Each file ID must be valid'),

    body('permanent')
        .optional()
        .isBoolean().withMessage('Permanent must be a boolean'),

    (req, res, next) => {
        console.log('Bulk Delete Files - Validation started');
        console.log('File IDs Count:', req.body.fileIds?.length);
        console.log('Permanent Delete:', req.body.permanent);
        handleValidationErrors(req, res, next);
    }
];

const searchFiles = [
    query('project')
        .optional()
        .isMongoId().withMessage('Invalid project ID format'),

    query('query')
        .notEmpty().withMessage('Search query is required')
        .trim()
        .isLength({ min: 1, max: 200 }).withMessage('Search query must be between 1 and 200 characters'),

    query('searchIn')
        .optional()
        .isIn(['name', 'content', 'both'])
        .withMessage('Search in must be name, content, or both'),

    query('caseSensitive')
        .optional()
        .isBoolean().withMessage('Case sensitive must be a boolean'),

    query('regex')
        .optional()
        .isBoolean().withMessage('Regex must be a boolean'),

    query('page')
        .optional()
        .isInt({ min: 1 }).withMessage('Page must be a positive integer'),

    query('limit')
        .optional()
        .isInt({ min: 1, max: 100 }).withMessage('Limit must be between 1 and 100'),

    (req, res, next) => {
        console.log('Search Files - Validation started');
        console.log('Search Query:', req.query.query);
        console.log('Query Parameters:', JSON.stringify(req.query, null, 2));
        handleValidationErrors(req, res, next);
    }
];

module.exports = {
    createTestFile,
    updateTestFile,
    getTestFile,
    deleteTestFile,
    listTestFiles,
    updateFileContent,
    lockFile,
    unlockFile,
    validateFile,
    compileFile,
    restoreFile,
    getFileVersions,
    revertToVersion,
    addSyntaxError,
    clearSyntaxErrors,
    updateMetadata,
    duplicateFile,
    moveFile,
    renameFile,
    getFileStatistics,
    bulkUpdateFiles,
    bulkDeleteFiles,
    searchFiles
};